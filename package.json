{
  "name": "slowify",
  "version": "1.0.0",
  "description": "Simple proxy to add latency to requests.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "http-proxy": "*"
  }
}
